// Endpoint es recibir un número y nos va a devolver un objeto, con dos propiedades. Nos va a devolver dos propiedades, la primera será decirnos si el número es par o impar, y la segunda será el número elevado al cuadrado.

import { isOdd, pow2 } from "./utils/tools.js";

console.log(isOdd(3));
console.log(pow2(3));